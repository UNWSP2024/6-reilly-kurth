#Start

#Function calculate_taxes(total_sales)
    #state_tax_rate = 0.05
    #county_tax_rate = 0.025
    #state_tax = total_sales * state_tax_rate
    #county_tax = total_sales * county_tax_rate
    #total_tax = state_tax + county_tax
    #Return state_tax, county_tax, total_tax

#Main Program
    #User to enter total sales for the month
    #Input total_sales

    #Call calculate_taxes(total_sales)
    #returned values in state_tax, county_tax, total_tax

    #Display "The amount of county sales tax is: " + county_tax
    #Display "The amount of state sales tax is: " + state_tax
    #Display "The total sales tax (county plus state) is: " + total_tax

#End
